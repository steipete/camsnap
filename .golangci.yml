run:
  timeout: 3m
  tests: true
  skip-dirs:
    - vendor

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gofmt
    - goimports
    - revive
  disable:
    - depguard

issues:
  exclude-use-default: false

severity:
  default-severity: error
